import React from 'react';
import styled from 'styled-components';

const AppContainer = styled.div`
  background-color: rgb(33, 33, 33);
  height: 100vh;
`;

const App = () => <AppContainer className="App" />;

export default App;
